1. 데이터베이스에서 구독자가져오기(action)
- action이다. 
- 왜냐하면 구독자는 계속 바뀌기 떄문에, 지금 가져온 구독자는 다음에 가져온 구독자와 다를 수 있다(실행시점의 의존)


2. 데이터베이스에서 쿠폰목록가져오기(action)
- action이다 
- 왜냐하면 데이터베이스틑 계속 바뀌기 때문에, 실행시점에 의존한다


3. 데이터베이스에서 가져온 구독자목록(data)
- "데이터베이스에서 구독자가져오기" 라는, action(event)에 대한 사실

4. 데이터베이스에서 가져온 쿠폰목록(data)
-  "데이터베이스에서 쿠폰가져오기" 라는, action(event)에 대한 사실


5. 보내야 할 이메일 목록 만들기 (calculate)
- 구독자 목록, 쿠폰 목록, 이메일 목록 으로 구성이 되어 있다
- 구독자 목록, 쿠폰 목록, 이메일을 인풋으로 받고, 외부의 효과를 받지 않고, output를 낸다

6. 이메일 전송하기 